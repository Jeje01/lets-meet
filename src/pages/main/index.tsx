"use client";

import { Calendar, type CalendarProps } from "antd";
import type { Dayjs } from "dayjs";

const Main: React.FC = () => {

  return (
    <div>
      <Calendar />
    </div>
  );
};

export default Main;
